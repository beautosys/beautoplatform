import { CustomPaginationDirective } from './custom-pagination.directive';

describe('CustomPaginationDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomPaginationDirective();
    expect(directive).toBeTruthy();
  });
});
